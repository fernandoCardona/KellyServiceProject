<div id="form">
  <h2>Edit Client Profile</h2>

  <form action="/client/edit" method="POST" enctype="multipart/form-data">

      <label>
        <input type="text" name="fullName" value="{{fullName}}" required/>
      </label>
      <label> 
        <input type="email" name="email" value="{{email}}" required/>
      </label>
      <label>
        <input type="password" name="password" placeholder="********" required />
      </label>
      <label>
        <input type="text" name="address" value="{{address}}" required/>
      </label>
      <label>
        <input type="number" name="postcode" value="{{postcode}}" required/>
      </label>
      <label>
        <p>Profile Image:</p>
        <input type="file" name="image" placeholder="Add image" />
      </label>

      <button type="submit"  class="btn btn-Client-login">Submit</button>

      {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
      {{/if}}
    </form>
</div>